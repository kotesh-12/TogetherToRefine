[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "18"

# SPA — send all non-file routes to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
